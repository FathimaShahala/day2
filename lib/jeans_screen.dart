import 'package:flutter/material.dart';

class JeansScreen extends StatelessWidget {
  const JeansScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Jeans Screen'),
      ),
    );
  }
}
